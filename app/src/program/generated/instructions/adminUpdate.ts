/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import { Serializer, struct, u64 } from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type AdminUpdateInstructionAccounts = {
  /** The current admin account. */
  admin: Signer;
  /** The new admin account. */
  newAdmin: PublicKey | Pda;
  /** The new treasury account. */
  newTreasury: PublicKey | Pda;
  /** The Locker account to be updated. */
  locker: PublicKey | Pda;
  /** The current treasury account that receives the fee. */
  treasury: PublicKey | Pda;
};

// Data.
export type AdminUpdateInstructionData = { newFee: bigint };

export type AdminUpdateInstructionDataArgs = { newFee: number | bigint };

export function getAdminUpdateInstructionDataSerializer(): Serializer<
  AdminUpdateInstructionDataArgs,
  AdminUpdateInstructionData
> {
  return struct<AdminUpdateInstructionData>([['newFee', u64()]], {
    description: 'AdminUpdateInstructionData',
  }) as Serializer<AdminUpdateInstructionDataArgs, AdminUpdateInstructionData>;
}

// Args.
export type AdminUpdateInstructionArgs = AdminUpdateInstructionDataArgs;

// Instruction.
export function adminUpdate(
  context: Pick<Context, 'programs'>,
  input: AdminUpdateInstructionAccounts & AdminUpdateInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'valhalla',
    'BgfvN8xjwoBD8YDvpDAFPZW6QxJeqrEZWvoXGg21PVzU'
  );

  // Accounts.
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: true as boolean,
      value: input.admin ?? null,
    },
    newAdmin: {
      index: 1,
      isWritable: false as boolean,
      value: input.newAdmin ?? null,
    },
    newTreasury: {
      index: 2,
      isWritable: false as boolean,
      value: input.newTreasury ?? null,
    },
    locker: {
      index: 3,
      isWritable: true as boolean,
      value: input.locker ?? null,
    },
    treasury: {
      index: 4,
      isWritable: false as boolean,
      value: input.treasury ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: AdminUpdateInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getAdminUpdateInstructionDataSerializer().serialize(
    resolvedArgs as AdminUpdateInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
