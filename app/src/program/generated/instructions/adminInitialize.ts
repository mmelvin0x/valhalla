/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import { Serializer, struct, u64 } from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type AdminInitializeInstructionAccounts = {
  /** The admin account that will sign the transaction. */
  admin: Signer;
  /** The locker account that will be initialized. */
  locker: PublicKey | Pda;
  /** The treasury account that receives the fee. */
  treasury: PublicKey | Pda;
  /** The system program account. */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type AdminInitializeInstructionData = { fee: bigint };

export type AdminInitializeInstructionDataArgs = { fee: number | bigint };

export function getAdminInitializeInstructionDataSerializer(): Serializer<
  AdminInitializeInstructionDataArgs,
  AdminInitializeInstructionData
> {
  return struct<AdminInitializeInstructionData>([['fee', u64()]], {
    description: 'AdminInitializeInstructionData',
  }) as Serializer<
    AdminInitializeInstructionDataArgs,
    AdminInitializeInstructionData
  >;
}

// Args.
export type AdminInitializeInstructionArgs = AdminInitializeInstructionDataArgs;

// Instruction.
export function adminInitialize(
  context: Pick<Context, 'programs'>,
  input: AdminInitializeInstructionAccounts & AdminInitializeInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'valhalla',
    'BgfvN8xjwoBD8YDvpDAFPZW6QxJeqrEZWvoXGg21PVzU'
  );

  // Accounts.
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: true as boolean,
      value: input.admin ?? null,
    },
    locker: {
      index: 1,
      isWritable: true as boolean,
      value: input.locker ?? null,
    },
    treasury: {
      index: 2,
      isWritable: false as boolean,
      value: input.treasury ?? null,
    },
    systemProgram: {
      index: 3,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: AdminInitializeInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getAdminInitializeInstructionDataSerializer().serialize(
    resolvedArgs as AdminInitializeInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
